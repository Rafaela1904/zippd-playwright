{
  "name": "zippd-playwright",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "@types/node": "^20.19.39",
    "dotenv": "^16.6.1",
    "typescript": "^5.2.2"
  }
}
